# TodoApp 使用ガイド

## アプリの起動

1. Android StudioでプロジェクトをOPEN
2. エミュレータまたは実機をセットアップ
3. Run (▶️) ボタンをクリック

## 機能説明

### 1. タスクの追加

1. 画面上部のテキストフィールドにタスク名を入力
2. 右下の「タスクを追加」ボタン（＋アイコン）をタップ
3. タスクが一覧に追加されます

### 2. タスクの表示

- タスクは作成日時の降順（新しいものが上）で表示されます
- 各タスクはカード形式で表示されます
- タスクが無い場合は「タスクがありません」と表示されます

### 3. タスクの削除

1. 削除したいタスクの右側にあるゴミ箱アイコンをタップ
2. タスクが即座に削除されます

## UIの説明

```
┌─────────────────────────────────────────┐
│  TodoApp                          [≡]   │ ← トップバー
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐ │
│  │  タスクを入力してください         │ │ ← 入力フィールド
│  └───────────────────────────────────┘ │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐ │
│  │  買い物に行く              [🗑]   │ │ ← タスクカード
│  └───────────────────────────────────┘ │
│  ┌───────────────────────────────────┐ │
│  │  レポートを書く            [🗑]   │ │
│  └───────────────────────────────────┘ │
│  ┌───────────────────────────────────┐ │
│  │  ジムに行く                [🗑]   │ │
│  └───────────────────────────────────┘ │
│                                         │
│                              ┌────────┐ │
│                              │ [+] タ │ │ ← 追加ボタン
│                              │  スク  │ │   (FAB)
│                              └────────┘ │
└─────────────────────────────────────────┘
```

## データの永続化

- すべてのタスクはRoomデータベースに保存されます
- アプリを閉じても、タスクは保持されます
- アプリを再起動すると、前回のタスクが表示されます

## 動作要件

- Android 7.0 (API 24) 以上
- インターネット接続不要（完全オフライン動作）

## トラブルシューティング

### ビルドエラーが発生する場合

1. Android Studioを最新版にアップデート
2. Gradle Syncを実行: File → Sync Project with Gradle Files
3. キャッシュをクリア: File → Invalidate Caches / Restart

### アプリが起動しない場合

1. エミュレータのAPI レベルを確認 (24以上必要)
2. Android StudioのLogcatでエラーログを確認
3. Clean Build: Build → Clean Project

### データが保存されない場合

- アプリケーションデータをクリアしてみてください
- Settings → Apps → TodoApp → Storage → Clear Data

## カスタマイズ

### アプリ名を変更
`app/src/main/res/values/strings.xml`の`app_name`を編集

### 色を変更
`app/src/main/java/com/example/todoapp/ui/theme/Color.kt`を編集

### データベース名を変更
`app/src/main/java/com/example/todoapp/data/AppDatabase.kt`の`todo_database`を編集

## 今後の拡張機能案

- タスクの編集機能
- タスクの完了/未完了ステータス
- タスクの優先度設定
- タスクの期限設定
- カテゴリ/タグ機能
- 検索・フィルタリング機能
- タスクの並び替え（ドラッグ&ドロップ）
- ダークモードの手動切り替え
- タスクのエクスポート/インポート
